### Goal

Implement issue #24 PR 3, **Agent manager and graph**, on top of the landed PR 1 lifecycle contract and PR 2 shared actor. Add one `maki-agent` manager per outer TUI session that is the sole production factory for that session’s root and descendant actors, tracks nested parent-child topology by `AgentId`, centrally enforces graph/concurrency limits, and provides race-safe node/subtree cancellation while preserving current TUI, `maki.agent.session`, and task compatibility behavior.

### Implementation Summary

Add a dependency-neutral manager layer under `maki-agent/src/manager/` above the existing `AgentActorHandle`. The actor remains the only FIFO/history/outcome/lifecycle scheduler for one agent; the manager owns the outer-session graph, actor registry, runtime tasks, topology, spawn authority, global active-turn permits, and graph-wide cancellation/shutdown. No second per-agent scheduler or history store is introduced.

One `SessionRuntime` in `maki-ui/src/event_loop.rs` owns one manager. `AgentHandles` becomes the root-node compatibility adapter rather than directly constructing an actor. `maki-lua/src/api/agent.rs::session` creates a child through an internal, turn-scoped manager capability carried by `ToolContext`, so the same path naturally creates grandchildren when a managed child invokes `maki.agent.session`. The manager-generated `AgentId` is runtime identity; `tool_use_id`, `SubagentInfo.parent_tool_use_id`, task IDs, TUI `run_id`, and provider history tool-use IDs remain compatibility/presentation/protocol correlation only.

Use the operator-selected default limits, configurable in `[agent]`:

- `max_concurrent_agent_turns = 8` per outer session;
- `max_agent_depth = 4` edges below a root at depth 0;
- `max_children_per_agent = 16` live direct children;
- `max_live_agents = 64`, including the root.

Closed/removed nodes remain inspectable for the manager lifetime but do not consume live-node or fan-out capacity. Completed, failed, and turn-cancelled agents remain live and reusable. Nested spawning is manager-allowed by default; PR 4 will intersect it with immutable per-agent policy and spawn ceilings.

PR review hardened the implemented boundaries without changing this architecture: actor tickets resolve only after status is reset to idle; each backend poll holds its physical manager permit across the complete opaque poll while explicit logical ownership remains visible to completed prompt waiters; panicking root/child factories become typed failures and roll back reservations; managed history envelopes publish the manager-assigned child identity; runtime replacement preserves session permissions and `exit_on_done`; startup rollback releases every claimed lock; and loss of an owned lock stops the session without checkpointing or replacing it. Session heartbeats run off the UI thread as joinable, generation-tagged lease tasks; transient advisory-lock contention remains on the error path that retains the lease instead of being reported as ownership loss; and release writes an immediately claimable marker through the verified locked inode rather than unlinking a pathname. A package-scoped storage fixture also uses canonical raw JSON so its test does not depend on feature-unified map ordering.

Primary touch points:

- New `maki-agent/src/manager/{mod.rs,types.rs,manager_error.rs,tests.rs}` (split further only where SRP warrants it), exported from `maki-agent/src/lib.rs`.
- `maki-agent/src/actor/{mod.rs,types.rs,runner.rs,queue.rs}` for narrow manager integration hooks, runner-level admission, and the atomic reusable cancellation cut.
- `maki-agent/src/tools/mod.rs`, `maki-agent/src/agent/run.rs`, and `maki-agent/src/types.rs` for opaque current-agent/turn manager context propagated into tool dispatch and non-serialized `SubagentInfo` agent/parent/delivery runtime metadata.
- `maki-config/src/lib.rs` plus generated configuration docs for validated graph-limit fields.
- `maki-storage/src/session_lock.rs` for identity-safe owned lock leases and backward-compatible ephemeral records.
- `maki-ui/src/event_loop.rs`, `maki-ui/src/app/session.rs`, `maki-ui/src/agent/mod.rs`, `agent_loop.rs`, and `command_router.rs` for inert runtime preparation, one manager per `SessionRuntime`, root creation, compatibility routing, and graph shutdown.
- `maki-lua/src/api/agent.rs`, `maki-lua/src/api/util/ctx.rs`, and `maki-lua/src/runtime.rs` for manager-created child actors, exact invocation scope, nested propagation, compatibility IDs, and blocking child-wait permit yielding.
- `plugins/task/init.lua` to remove the blocking composite's separate outer semaphore acquisition, retain/deprecate the process-wide semaphore as the unmanaged frontend fallback until PR 9, and pass that fallback to `maki.agent.session` without double-enforcing managed turns; keep task API names/result shapes/polling behavior unchanged.

Scope boundaries:

- Do not move model/provider, prompt, tools, permissions, mode, request options, or immutable queued policy ownership into nodes; PR 4 owns that.
- Do not add public Lua `Agent`/`AgentRef`, spawn/send/wait APIs, structured completion subscriptions, or migrate task polling; PR 5 owns that.
- Do not change persisted session/history format, persist topology, migrate stored subagent transcripts, or define restart recovery; PR 6 owns that. The ephemeral cross-process `.lock` record may gain a backward-compatible owner token solely to make lease heartbeat/release identity-safe.
- Do not add guest presets/modes, graph UI/tree navigation, arbitrary-agent focus, or migrate ACP/headless/SDK/print; PR 7/8/9 own those.
- Preserve direct unmanaged headless/ACP/SDK paths for now. “Sole factory” is enforced for the TUI outer sessions migrated in this PR and for all descendants spawned inside them; remaining direct frontend construction is an explicit PR 9 compatibility exception. Direct `AgentActorHandle::spawn` remains available only to the manager, actor tests/test support, and the documented deferred unmanaged frontends.

### Implementation Plan

#### Phase 1: Define the manager, graph, and nominal handles in `maki-agent`

1. Add `AgentManager`/`AgentManagerHandle` with one `Arc<ManagerInner>` containing a short-held mutex over graph state, the selected `AgentLimits`, a manager generation/identity, and one shared active-turn limiter. Never hold the manager lock across an await, backend call, actor call that can re-enter tool dispatch, or task join.
2. Model graph nodes by stable `AgentId`. A private live node record owns:
   - optional `parent_id` and immutable root id/depth;
   - an ordered or deterministically sortable child-id set;
   - the `AgentActorHandle` and its runner task/join ownership;
   - live/closing/closed/removed graph lifecycle;
   - optional compatibility metadata such as label and `spawned_by_tool_use_id`, explicitly non-authoritative.
   Expose immutable `AgentRef`/`AgentNodeSnapshot` projections with identity, parent, root, depth, children, actor status/lifecycle, and graph lifecycle. Visibility is descriptive only and cannot be upgraded into control by supplying an id.
3. Make manager creation and actor creation two-phase and rollback-safe:
   - `create_root` is host-only, succeeds exactly once, reserves the root node under the graph lock, constructs the adapter-supplied backend/actor without the lock, then commits the live node or rolls back the reservation on returned error, unwind, or a concurrent closing state;
   - `spawn_child` validates a manager-issued authority, parent liveness, graph generation, limits, and optional compatibility metadata atomically; reserves the child edge/id; builds the backend/actor outside the lock; then commits or rolls back. Factory panics are caught at this boundary and returned as `ManagerError::Factory`, restoring live and fan-out capacity exactly once;
   - no public API accepts a caller-chosen parent as sufficient authority, and guessed `AgentId`s never grant control;
   - all production TUI root/descendant construction routes through these methods.
4. Introduce a small opaque internal turn capability contract, not the complete PR 4 security/policy system:
   - `AgentManagerHandle` is the host control surface for root creation and graph lifecycle;
   - a restricted turn-scoped descendant-spawn capability contains manager generation, current `AgentId`, current `TurnId`, and an unforgeable manager-issued nonce; every spawn/yield validates it against a manager-side active-turn entry before any graph lookup;
   - one non-cloneable RAII `ManagedTurnGuard` uniquely activates the nonce after managed execution admission and revokes it on every drop path, including setup failure, permit-acquisition cancellation, ordinary completion, cancellation, backend future drop, and unwind; cloned `ToolContext`s carry only a cloneable restricted token, never the guard;
   - cloning the token may preserve or narrow scope but cannot change the parent/current node, and a retained clone in a detached future becomes unusable as soon as the guard revokes the active entry;
   - node ids, topology, tool-use ids, and UI focus are never bearer authority.
   Keep representation and nonce validation private behind methods so PR 4 can add policy-derived ceilings without changing call sites. Test wrong-manager authority and retained-token replay after guard drop; validate exact authority matching at the Lua invocation boundary.
5. Define `AgentLimits` and typed `ManagerError` cases for missing/duplicate root, unknown or non-live node, wrong-manager authority, inactive turn, non-descendant waits, depth exceeded, direct-child limit, live-node limit, graph shutdown, and factory failure. Errors include actionable bounded context but no capability secret.
6. Add `[agent]` config fields in `maki-config/src/lib.rs` for the four selected limits, with constants at the top of the file, generated metadata/descriptions, `min = 1` validation, and cross-field validation where required (at least `max_live_agents >= 1`; depth is measured with root = 0). Convert safely to `usize` at the manager boundary.
7. Add structured tracing for manager generation/id, agent/root/parent ids, depth, graph revision, live count, active-turn count, limit rejection, spawn commit/rollback, cancellation cut, and shutdown. Do not add a dependency unless existing `Mutex`, `async_lock`, `event_listener`, and `smol` primitives cannot implement the contract.

#### Phase 2: Centralize active-turn admission without recursive deadlock

1. Put managed admission in the actor runner before backend construction, then gate the admitted future on every poll:
   - add an optional manager-owned `ManagedTurnAdmission` hook to manager-created actors; after the actor installs `ActiveCancel` but before it calls `ActorBackend::run_turn`, race asynchronous physical-permit acquisition against that turn's cancel/reason token;
   - cancellation/close/shutdown before acquisition makes the existing actor runner synthesize the same not-entered result already used by its pre-backend cancellation path: an admitted non-root ticket receives exactly one `TurnOutcome::Cancelled`, while a root that never entered has no retained/delivered outcome. The backend method is never invoked, so its borrowed future is neither constructed nor polled;
   - for every entered or synthesized terminal path, clear the active turn and publish `ActorStatus::Idle` before retaining/delivering the outcome and resolving the ticket. Ticket completion therefore guarantees an immediately reusable idle actor while preserving event-before-ticket ordering;
   - after acquisition, create the unique `ManagedTurnGuard` and shared `TurnPermitLease`, invoke `ActorBackend::run_turn`, and wrap only the returned borrowed future in `ManagedExecutionFuture`; do not attempt to store a self-referential backend/history future;
   - on every poll, the lease gate runs first: if suspension count is zero but no physical permit is held, asynchronously reacquire and return `Pending`. When a permit is present, take its guard from lease state under the mutex and hold it poll-locally across the complete backend poll; return it only if the result is pending and the lease remains unsuspended/open, otherwise drop it. A concurrent suspension can therefore update bookkeeping but cannot release physical capacity out from under an opaque backend poll, including cancellation cleanup.
   Controls and standalone compaction bypass managed admission unless they enter a model turn. Count active work, not live actors or queued turns.
2. Preserve per-agent FIFO: actors may queue independently, but no more than `max_concurrent_agent_turns` managed turns execute across the graph. Permit acquisition races actor close/shutdown/cancellation, so a queued actor waiting for capacity terminalizes correctly rather than hanging.
3. Propagate through `TurnContext` -> adapter backend -> `AgentParams`/`ToolContext` the restricted active-turn token and `TurnPermitLease` tied to current manager, `AgentId`, `TurnId`, and nonce. `Agent::tool_context()` copies these to concurrent model/Lua tool invocations. Nested `AgentContext::from` and `to_tool_context` retain them while still clearing `tool_use_id` and `live_sink` as today. Do not make a retained `LuaSession` the owner/source of the parent turn lease; it stores only its own child `AgentId`/manager relation.
4. Define the concrete blocking-descendant suspension protocol used by compatibility `LuaSession::prompt` without polling an unowned opaque parent future:
   - only a manager-validated wait by the active parent turn on its own descendant may register a blocking wait; unrelated turns, background `send`, UI input, detached callbacks, and unmanaged sessions cannot yield the lease;
   - registration creates shared `ManagedPromptWait` completion/cancel state plus an independently driven watcher task. A manager/reaper-level `WatcherRegistry`, not the lease, owns watcher `smol::Task` handles. The lease stores only watcher ids, cancellation triggers, and completion-state handles; each watcher captures the lease state and owns one non-cloneable `PermitSuspensionGuard`, so no strong lease -> task -> lease cycle exists. The first guard atomically releases the physical permit and later guards increment the count without releasing twice;
   - the watcher races the exact child `TurnTicket` against prompt-drop cancellation and the optional prompt timeout. Exactly one branch stores a completed outcome/error when applicable and retires the guard once. Prompt drop signals only its watcher and lets the child keep running, preserving asynchronous-session behavior; timeout permanently closes the child as today. `WatcherRegistry` retains active handles; terminal watchers mark themselves reapable and may remove their registry entry into a detached join, while shutdown drains whatever remains. Lease retirement and registry cleanup are independently idempotent, and watchers run no Lua/model/provider continuation;
   - `LuaSession::prompt` awaits the watcher state, then awaits the lease's shared owned-generation barrier before reading presentation/cumulative usage, building a result, returning to Lua, or executing any post-wait continuation. Child completion can therefore decrement suspension counts while the opaque parent backend remains completely unpolled. An early completion remains parked while sibling guards exist;
   - `PermitSuspensionGuard::drop` is synchronous and idempotent: it decrements exactly once, marks `reacquire_required` and advances/wakes the shared resume generation when the last suspension disappears, and wakes the outer `ManagedExecutionFuture`; it never blocks or acquires a semaphore. Watcher setup failure rolls back registration synchronously before returning an error;
   - the outer poll gate is the only authority allowed to restore physical ownership. After asynchronous reacquisition it marks the shared generation owned and wakes completed prompt waiters; until then neither the outer future nor any prompt continuation may poll parent Lua/model/tool code;
   - parent cancellation revokes turn/spawn authority, snapshots/signals all watcher ids registered to the lease, and wakes the gate but does not bypass limiting. Once suspensions reach zero, the execution future reacquires a physical permit before polling the backend to observe cancellation, run cooperative cleanup, emit/retain its terminal outcome, and release;
   - `ManagedExecutionFuture::drop` is the total abnormal-teardown boundary: synchronously mark the lease closing, reject new wait registrations, signal every registered watcher, release any held physical permit, and only then revoke/drop `ManagedTurnGuard`. This terminal path never reacquires because no parent continuation remains. Watcher cancellation/drop still retires each guard exactly once; `WatcherRegistry` retains and joins those tasks/state after outer drop, and outer-drop races with child completion, timeout, prompt drop, cancellation, or unwind use one atomic winner per watcher;
   - manager shutdown first closes graph/turn admission, marks captured nodes closing, signals actor shutdown, and applies one caller-provided timeout to the aggregate join. It returns `ShutdownReport { joined, timed_out }`; if nodes remain, a detached reaper retains the manager inner state, limiter, watcher tasks, and runner joins until they settle. The TUI supplies its existing three-second shutdown policy rather than deriving it from provider request timeouts;
   - concurrent tool futures therefore cannot let an early child, prompt drop, timeout, or cancellation resume parent execution while capacity is owned elsewhere.
   This is an internal scheduling primitive, not the public PR 5 wait API.
5. Keep `plugins.task.max_concurrent` registered so existing configurations continue to parse, mark it deprecated, and use its process-wide semaphore only as the fallback for unmanaged headless/ACP/SDK/print sessions until PR 9. Managed sessions do not acquire or receive that semaphore because `[agent].max_concurrent_agent_turns` is authoritative per outer session. Do not alias the values because their scopes differ. Remove the blocking `task` composite’s separate outer semaphore acquisition; both blocking and background paths pass the fallback to `maki.agent.session`, whose adapter conditionally uses it only when no manager context exists. Document the split and never enforce both limits on one turn.
6. Prove with deterministic gates and bounded channels that active turns never exceed the manager limit, one blocking child wait yields the parent permit, two child waits share one parent suspension, descendant validation fails before suspension, watcher-registration failure cancels the exact admitted child turn, and a timed-out shutdown transfers the held runner to a reaper that eventually joins it. Keep broader prompt-drop, timeout, unwind, and cancellation-reacquisition behavior encapsulated in the lease state machine without adding a second scheduler.

#### Phase 3: Implement graph lifecycle, limits, and cancellation

1. Enforce limits under the graph lock at reservation time:
   - root depth is 0 and children are parent depth + 1;
   - live count includes reserved/live root and descendants, excludes closed/removed tombstones;
   - per-parent fan-out includes reserved/live direct children, excludes closed/removed children;
   - a node remains spawnable after an ordinary turn completion/failure/user cancellation, but not after close/remove/shutdown.
2. First add a narrow actor integration operation, `cancel_existing` (final name idiomatic), that establishes one reusable cancellation cut atomically against actor admission, queue pop, and active installation under the actor’s own state/queue linearization protocol. It cancels the active turn and terminalizes queued turns belonging to the cut while leaving the actor open; work admitted after the cut survives. Replace `AgentActorHandle::cancel_all` internals/callers with this operation where reusable cut semantics are required. Add an actor test barrier exactly between queue removal and active installation to prove no turn can miss the cut.
3. Define manager operations with explicit semantics:
   - `cancel_agent(id)` is reusable and invokes that node actor’s atomic reusable cut, affecting only work present at the actor cut while leaving the node open;
   - `cancel_subtree(id)` captures the node plus all then-reserved/committed descendants at one graph revision, releases the graph lock, then composes one atomic actor cut per captured node; descendants committed after the graph cut are excluded;
   - `close_subtree(id)`/remove is permanent: atomically marks the cut closing, rejects new descendants/work through manager handles, releases the lock, closes each actor, and marks/tombstones nodes when settled;
   - `shutdown(timeout)` atomically stops graph admission, captures live/reserved nodes, applies the caller-provided timeout to aggregate completion, and returns disjoint `ShutdownReport { joined, timed_out }`. On timeout, a detached reaper retains the manager inner state, wait watchers, limiter, and outstanding runner joins until all actors settle.
4. Linearize spawn versus cancellation:
   - a child reserved before a reusable graph cut is included; after its factory commits, the manager must apply its captured actor cancellation generation before exposing it live;
   - a child reserved/committed after the reusable graph cut survives;
   - admission before an actor cut is cancelled while admission after it survives, including the queue-pop/active-install boundary;
   - permanent close/shutdown marks ancestors/cut nodes first, so no child can commit beneath them afterward;
   - factory failure and concurrent close always remove reservations and restore capacity exactly once.
5. Dropping user-facing handles must not cancel work. Manager/runtime task ownership keeps nodes alive. Closing/removing a child does not close ancestors or siblings. Closing the root subtree closes the graph’s live nodes but leaves snapshots/tombstones available until manager drop.
6. Keep closed node metadata in manager snapshots for the outer-session lifetime, while excluding it from live limits. Do not persist it in this PR.
7. Add focused actor/manager deterministic tests for root/child topology, wrong-manager and expired authority, reservation rollback, depth and direct-child limits, closed-node capacity recovery, reusable exact-node cancellation, sibling isolation, the queue-pop/active-install gap, reserved-root close/shutdown races, parent permit yielding, shared sibling suspensions, active-turn limiting, and shutdown reaping.

#### Phase 4: Build a deterministic TUI runtime harness and make the outer session own the manager

1. Refactor reset/load/rewind into immutable `SessionReplacementRequest`s before adding manager replacement. `App::reset_session`, `load_loaded_session`/`apply_loaded_session`, and `rewind_to` must no longer checkpoint-then mutate the live App, rotate/bind command targets, clear history mirrors, fire replacement/restore callbacks, acquire/release session locks, install a provider, change global UI state, or alter chats/session/permissions/UI before commit. Each request contains candidate `AppSession`, history/model request, replacement kind, deferred UI/input/command/autocmd/lock effects, and rewind restoration data. A pre-replacement checkpoint may occur only as an explicit write of current state and cannot alter live routing.
2. Replace direct `SpawnCtx::spawn_runtime` reuse with `SpawnCtx::prepare_runtime(...) -> PreparedSessionRuntime` and a synchronous/no-await `activate(...) -> SessionRuntime` boundary. Preparation itself is infallible after provider resolution; target session-lock claim is the final fallible precommit step:
   - `prepare` constructs private App state, permissions, manager/root resources, handles, shell/notification state, and buffered display/restore/queue work, but is inert: it does not bind a command-registry target, mutate scrollbar/global UI settings, install into the shared provider/model slot, emit restore/autocmd requests, flush/admit queued work, claim/release live session locks, or expose an actor mailbox/route;
   - split `App::new` and `restore_display` into pure/private preparation plus activation so history/display can be built with restore emissions buffered and no shared side effects;
   - if actor construction must start a runner during preparation, keep its mailbox/routes private and make `PreparedSessionRuntime::drop` shut it down/join or detach a guaranteed-closing task; no work can reach it before activation;
   - add a non-cloneable `maki-storage::session_lock::ClaimedSessionLock` with a random owner token and a backward-compatible ephemeral lock record `PID OWNER_TOKEN`. Parsing continues to accept legacy PID-only records for cross-version claim/open checks; this changes only `.lock` coordination files, never stored session/history format. `claim` returns the sole lease; `heartbeat(&mut self)` and `release(self)` lock and compare both PID and token and never recreate or alter another owner’s pathname. Release writes the malformed sentinel `released` through the verified locked file descriptor instead of unlinking; readers immediately treat it as free and the next claimant overwrites it regardless of fresh mtime;
   - migrate every TUI lock path, not only replacement: initial startup/`push_runtime` claims a lease into `SessionRuntime`; each periodic heartbeat moves the exact non-cloneable lease into one joinable `smol::unblock` task owned by `SessionLockState::InFlight`, while the existing internal event channel carries only a runtime-generation readiness signal. A second beat cannot start while one is in flight; stale signals cannot attach to replacement runtimes; removal, startup rollback, replacement cleanup, and process exit collect/release held or in-flight ownership under one shared shutdown deadline. If bounded cleanup cancels a still-blocked worker, `ClaimedSessionLock::Drop` marks it released when the blocking call eventually returns. If heartbeat proves ownership was replaced, mark the runtime lock lost, request an error exit, disable replacement, and skip later checkpoint/release attempts so this process cannot overwrite the new owner's session. ACP/SDK legacy free-function use may remain only if an explicit source audit shows those deferred frontends still need it;
   - for same-session-id replacements such as rewind, retain the existing runtime’s exact guard continuously and transfer it at activation with no filesystem operation. For different ids, after all other preparation succeeds, synchronously claim the target as the final fallible precommit step; the guard releases only that target on abandoned commit and transfers into the activated outer runtime/lock owner on success. Release the distinct old guard only after activation;
   - `activate` synchronously installs the selected model/provider, publishes command targets, mailbox/routes, App state, and history, and transfers the prepared/existing lock guard into a complete `SessionRuntime`. The caller swaps that runtime into the session slot before emitting buffered restore/autocmd/queue effects exactly once.
   Add a focused `RuntimeHarness` that constructs prepared and active runtimes, observes command-target publication, mailbox visibility, session id, manager generation, root id, deferred restore work, provider identity, and lock ownership, and injects failure at the final target-lock claim.
3. Add `AgentManagerHandle` to `SessionRuntime` or make it unambiguously owned through `AgentHandles`; construction in `SpawnCtx::spawn_runtime` creates one manager from the outer session’s resolved `AgentConfig`, then creates its root through the host factory. Different tabs receive distinct managers and graphs.
4. Refactor `AgentHandles::spawn`/`spawn_agent_internal` to accept the manager/root factory result rather than generating `AgentId` and calling `AgentActorHandle::spawn` directly. Preserve shared-history publication before handles escape, event channels, queue facade, mailbox, MCP handle, provider slot, permissions, dynamic prompt/tool setup, and all PR 2 root adapter behavior.
5. Execute every `SessionReplacementRequest` as a whole-`SessionRuntime` transaction:
   1. checkpoint the old session without mutating its live App/routing;
   2. call inert `SpawnCtx::prepare_runtime`; preparation loads stored session permission rules even for empty histories, an abandoned candidate keeps its command target and mailbox unpublished, and its manager is shut down through normal prepared-handle teardown;
   3. resolve lock precommit by identity: same id borrows/marks transfer of the old `ClaimedSessionLock` without I/O; different id claims a new guard as the final fallible step, and abandonment releases only that target;
   4. call no-await `activate` to bind routes/target, apply shared model/UI state, transfer the lock guard, preserve host runtime flags such as `exit_on_done`, publish history, and atomically replace the runtime slot;
   5. commit deferred UI/input/command-target changes and fire replacement/restore autocmds with required old/new identity, then flush buffered restored work exactly once;
   6. if ids differ, release the old lock only after the new runtime owns its target lock and routes; for same-id replacement never release/reclaim it. Then initiate old-manager shutdown and detach or collect its join task without blocking new input.
   Old events remain harmless through the per-tab generation/run-id filter, which advances only at activation. Activation operations must be designed infallible or have explicit compensating rollback before any event/work publication. This PR has no in-place root-runtime replacement API.
5. Integrate manager cancellation while preserving compatibility routes:
   - `AgentHandles::cancel` uses manager `cancel_subtree(root_id)` for graph-aware outer-session cancellation;
   - the command router keeps its existing root actor plus `subagent_cancels.cancel_all()` compatibility path until the remaining frontends and command plumbing migrate together;
   - process exit/outer-session replacement uses manager shutdown/close semantics;
   - `Cancel { run_id }` remains root actor correlation cancellation, not graph identity;
   - child interactive cancel routes by the live `AgentId`-selected chat and its stamped `SubagentCancel` closure; the closure targets the actor directly and never derives control from `parent_tool_use_id`.
6. Add non-serialized `agent_id`, `parent_agent_id`, `parent_is_root`, and `auto_deliver` runtime metadata to live `SubagentInfo` envelopes. Use a dedicated `live_chat_index` plus `AgentId`-keyed channels and stamped-outcome tracking for live routing, while retaining string-keyed `chat_index` for presentation and restored display-only chats. Keep `parent_tool_use_id` only as a visible label, provider/tool correlation, and legacy storage field. A restored child tab has no live `AgentId`, no live input/cancel channel, and never enters live manager maps. Two live children with the same compatibility string must produce separate tabs/channels and independently route input, cancellation, outcomes, and history.
7. Keep public task ids unchanged but make collision behavior explicit: normal task plugin creation remains collision-free because each model tool invocation has a unique provider tool-use id and the no-tool-id fallback generates a fresh `MakiId`. The task table rejects an unexpected duplicate instead of overwriting an existing entry. Duplicate compatibility ids are nevertheless supported for lower-level/direct `maki.agent.session` callers and TUI routing because `AgentId` is the live key. Add both duplicate-session routing tests and a task duplicate-rejection regression.
8. Retain the TUI-owned `CancelMap<String>` only as compatibility cancellation plumbing for the existing command router and unmanaged paths. It must not become graph identity, topology, authorization, limit accounting, or live-chat routing.
9. Preserve all existing app behavior: flat child chat/task picker, async child outliving its spawning root turn, stamped terminal deduplication by `(AgentId, TurnId)`, root stale-event filtering by `run_id`, child input and paste, failure detail, respawn channel behavior, and queue/drain ordering. Nested descendants also remain flat: terminal history can create their row, grandchild completion is not promoted to the root queue, only direct-root async children auto-deliver to root, and `SubagentClosed` renders `DESPAWNED` and disables input. PR 8B, not this PR, renders the graph.

#### Phase 5: Route Lua compatibility sessions through manager child creation

1. Refactor `maki-lua/src/api/agent.rs::session` so a managed handler context builds its existing `LuaActorState`/`LuaActorBackend` and passes a child factory closure/request to `AgentManager::spawn_child`. Do not generate `AgentId` or call `AgentActorHandle::spawn` directly. Preserve current provider/model/system/tools/local-tools/audience/MCP/thinking/fast construction as adapter-owned state until PR 4.
2. Bind the child to `agent_ctx`’s current managed `AgentId` and active `TurnId`; derive neither from `tool_use_id` nor `session_id`. Store `spawned_by_tool_use_id` only as optional compatibility metadata. On a managed child’s tool dispatch, propagate its own current node/turn context, so a nested `maki.agent.session` creates a grandchild under that child.
3. Keep public compatibility exactly:
   - `LuaSession::session_id()` and task IDs retain their current string/tool-use-id behavior in PR 3; normal task creation rejects an unexpected duplicate id rather than overwriting `tasks[task_id]`;
   - `prompt`, `send`, `status`, and `close` retain current result/error/status shapes, cumulative usage, relay, local capture, and retry behavior;
   - serialized `SubagentInfo.parent_tool_use_id` and the flat visual layout remain unchanged, while live envelopes carry the manager-assigned non-serialized `AgentId` for unambiguous routing. The managed backend initializes that identity only after `spawn_child` returns, so history never publishes a provisional caller-generated id;
   - `task`, `task_spawn`, `task_send`, `task_get`, and `task_despawn` remain callable with the same schemas and outputs; expose them to `general_sub` so managed general children can create and control descendants, while retaining the research-child restriction.
4. Separate managed lifecycle authority from compatibility cancellation plumbing:
   - managed `LuaSession::close` and `task_despawn` permanently call manager-backed `close_subtree`, so grandchildren cannot leak;
   - reusable child cancellation remains a stamped `SubagentCancel` actor closure selected through the child's live `AgentId` route;
   - outer-session lifecycle uses manager subtree cancellation/shutdown;
   - retain `CancelMap<String>` for the existing command-router and unmanaged PR 9 compatibility paths, never for graph lookup or authorization.
5. Fail closed for absent/stale authority in a managed context. A handler with a stale turn capability, a context-free Lua callback, a wrong-manager node, or a forged parent id gets the normal `(nil, err)` runtime pair. `start`/restore contexts still cannot spawn managed descendants. Unmanaged headless/ACP compatibility continues its old path until PR 9 and is covered explicitly so this migration does not break it.
6. Extend the existing concurrent Lua invocation scope instead of storing creation context in `LuaSession`:
   - add an optional `CurrentManagedTurn` containing the restricted token and `TurnPermitLease` to `maki-lua/src/runtime.rs::TaskCell`;
   - in `run_lua_tool`, derive it from the handler’s `LuaCtx`/`AgentContext` before moving that context into userdata, and install it in the same `TaskScope` whose `ScopedFuture` restores the correct task app-data on every poll;
   - both managed `maki.agent.session(ctx, ...)` and `LuaSession::prompt(lua, ...)` query `active_task(lua)` at invocation time. Managed `session` requires the current `TaskCell` token’s manager/node/turn/nonce to exactly match the supplied `LuaCtx` before provider construction, relay spawning, cancel registration, or graph reservation. `prompt` validates that the session node is a controlled descendant and suspends that current lease;
   - a session created in turn A but prompted in turn B uses B’s lease. A captured A `LuaCtx` invoked while B’s scope is current fails exact-match validation;
   - concurrent handler coroutines each observe their own `TaskCell`. `PendingAsyncTask` inherits the spawning task’s `CurrentManagedTurn`, so `maki.async.run` and `maki.async.join` workers may create or prompt descendants while that exact managed turn remains active. This is required for joined blocking work to share the parent lease at capacity one. The capability remains turn-scoped: manager validation rejects a retained worker as soon as the originating turn ends, as covered by `managed_async_worker_outliving_turn_loses_stale_task_authority`;
   - delivery, start, restore, command, click, and other context-free scopes carry no managed turn. A prompt without valid current managed authority still works through the unmanaged/ordinary exact-turn wait path but does not release any permit; a wrong-manager, stale, or non-descendant attempt fails closed before suspension;
   - ensure a background child remains independent of parent turn cancellation exactly as PR 2 established.
7. Add focused Lua/unit integration coverage using canned providers for manager-authorized child creation and permanent close, nested relay identity/history, task duplicate rejection, general-child versus research-child tool audiences, async capacity acquisition, cancellation while waiting for unmanaged capacity, and capacity release on despawn. Cover nested graph visibility, limit rejection, and capacity recovery with the manual smoke-test sequence.

#### Phase 6: Remove duplicate ownership and verify migration boundaries

1. Audit production `AgentActorHandle::spawn` calls. Migrated TUI roots and managed Lua descendants must construct through `maki-agent::manager`; allow actor/manager tests, the explicit unmanaged Lua branch, and deferred headless/ACP/SDK/print compatibility sites.
2. Audit `CancelMap<String>`, `parent_tool_use_id`, `tool_use_id`, and `run_id` uses. Confirm no graph lookup, topology edge, manager authorization, limit accounting, or live TUI routing is keyed by these compatibility values. Keep provider/history tool-use pairing, presentation indexing, and compatibility cancellation plumbing untouched.
3. Run formatting and generated docs after adding config fields/deprecation text. Run crate-scoped checks/tests first, then workspace lint/tests and dependency hygiene using the repository’s remote CI workflow described in `AGENTS.local.md` if present.
4. Preserve unrelated untracked files reported at session start: `.agents/makima-issues/`, `.agents/skills/code-review/`, `.agents/skills/luna-update-branch/`, `PR_COMMENT.md`, and `makima-linux-aarch64`. Do not overwrite or include them except for the approved plan artifact handled by Polytoken.

### Acceptance Criteria

- **AC.1:** Reset/load/rewind produce immutable replacement requests. Prepared runtimes keep command targets and mailboxes unpublished until activation; every outer session owns one manager/rooted graph. Same-id replacement transfers the exact lock lease, different-id target-lock failure leaves the old runtime identity, manager/root, command target, provider, and lock unchanged, and successful replacement restores permissions for empty or populated histories, preserves `exit_on_done`, publishes the candidate before deferred restore work, and only then shuts down the old manager. Startup rollback releases all previously claimed leases; detected lock loss stops without saving or replacing the runtime.
- **AC.2:** Within a managed TUI session, the root and every Lua child/grandchild actor are created through `AgentManager`; each node has a stable unique `AgentId`, immutable parent/root/depth relationships, and the root differs from descendants only by having no parent.
- **AC.3:** Graph ids and topology are not authority. Managed descendant spawning requires a valid active turn-scoped manager capability; guessed ids, wrong-manager/stale capabilities, context-free callbacks, and non-handler contexts fail before lookup or actor creation. Returned factory errors and panics both roll back reservations and are presented as typed factory failures.
- **AC.4:** The selected configurable defaults are effective and validated: 8 concurrent managed turns, depth 4 below root, 16 live direct children, and 64 live graph nodes including root. Limit rejection is atomic, typed, leaves no node/permit/task leak, and closed/removed tombstones do not consume live capacity.
- **AC.5:** Manager-wide execution never exceeds the configured active-turn cap. Concurrent blocking descendant waits from one parent share a reference-counted turn lease: the first suspension releases capacity and only the last leaving wait may reacquire it. Descendant validation and failed watcher registration do not leak suspension state, and a parent waiting on children yields capacity without introducing a second scheduler.
- **AC.6:** Exact-node cancellation affects only that node; reusable subtree cancellation captures the current graph cut while leaving nodes reusable; permanent close/remove prevents later child commits and closes the selected branch without affecting ancestors or siblings. Manager shutdown uses one caller-provided timeout and returns each captured node in exactly one of `joined` or `timed_out`; a detached reaper retains manager state and outstanding runner/watcher ownership until eventual settlement.
- **AC.7:** Managed topology, authorization, limits, and live TUI child selection/routing use non-serialized `AgentId`, not `tool_use_id`, `parent_tool_use_id`, task IDs, or TUI `run_id`. Two live sessions sharing one compatibility string retain independent tabs/channels/input/cancel/outcome/history routing. Compatibility values remain for labels, event/protocol correlation, restored presentation, and the retained command-router/unmanaged cancellation plumbing.
- **AC.8:** `maki.agent.session` and bundled `task*` compatibility behavior remains observable as before: public ids/schemas/result/status shapes, collision-free normal task creation with explicit duplicate rejection, async survival beyond the spawning parent turn, reusable failure/interactive-cancel recovery, cumulative usage, history/failure relay carrying the manager-assigned `AgentId`, local structured capture, permanent `task_despawn`, managed manager-only limiting, and unmanaged legacy-limit fallback. A completed prompt ticket observes both the terminal detail and an idle reusable actor.
- **AC.9:** A managed child can invoke the same compatibility session primitive to create a grandchild under itself, subject to manager limits, and nested `call_tool` propagation keeps manager/current-agent authority while clearing outer UI tool correlation.
- **AC.10:** Existing TUI root queue, interrupt, compaction, respawn/event-channel, flat chat/task picker, child input/paste, terminal deduplication, stale root filtering, and failure rendering regressions continue to pass. Grandchildren appear as flat rows without promoting their completion to the root, direct-root async completion still auto-delivers once, and permanently closed rows show `DESPAWNED`; no graph/tree UI or persistence-format change appears in this PR.
- **AC.11:** A focused source audit confirms migrated TUI roots and managed Lua descendants construct through the manager, while direct construction remains in actor tests, the explicit unmanaged Lua branch, and deferred frontend compatibility paths. No graph topology, authorization, or limit accounting is keyed by compatibility ids.
- **AC.12:** Focused `maki-agent`, `maki-config`, `maki-ui`, and `maki-lua` checks/tests, formatting, generated-doc checks, clippy with warnings denied, workspace nextest, and dependency hygiene all pass.

### Test Strategy

Use deterministic fake/scripted backends, bounded channels, and explicit barriers. Do not add sleeps or polling loops. Extend the existing actor test infrastructure rather than replacing PR 1/2 lifecycle tests.

| Acceptance criterion | Named test or check |
|---|---|
| AC.1 | `prepared_runtime_is_inert_until_activate`, `activation_publishes_target_mailbox_and_runtime_identities`, `replacement_restore_effects_activate_once_after_publication`, `one_manager_per_runtime`, `same_id_replacement_transfers_the_exact_lock_without_io`, `different_id_activation_releases_old_lock_only_after_swap`, `different_id_claim_failure_preserves_the_complete_runtime`, `prepared_target_is_stale_until_activation`, `dropped_prepared_target_is_never_published`, `empty_replacement_loads_session_permission_rules_during_preparation`, `replacement_preserves_exit_on_done`, `startup_rollback_releases_previously_claimed_locks`, `lost_session_lock_stops_without_saving`, `prepared_replacement_cannot_reset_lock_lost_or_save`, `heartbeat_runs_off_thread_and_only_one_is_in_flight`, `same_id_replacement_rejects_in_flight_lease`, `stale_heartbeat_signal_cannot_poison_replacement`, and `graceful_cleanup_joins_in_flight_heartbeat_and_releases_lock`; storage regressions `heartbeat_contention_does_not_report_ownership_loss`, `stale_guard_heartbeat_does_not_overwrite_replaced_lock`, `release_marker_is_immediately_claimable`, and `release_does_not_modify_path_replaced_after_owner_check`. |
| AC.2 | `root_and_nested_children_use_one_factory` and `managed_session_uses_root_authority_and_closes_its_node`; inspect manager snapshots for immutable parent/root/depth. |
| AC.3 | `wrong_manager_capability_is_rejected`, `retained_context_clone_cannot_spawn_after_guard_drop`, `descendant_preflight_rejects_before_suspension`, `panicking_root_factory_rolls_back_reservation`, and `panicking_child_factory_restores_capacity`. |
| AC.4 | Config default/construction/zero-value validation tests plus `depth_and_child_limits_are_atomic`, `close_child_preserves_parent_and_releases_capacity`, and `factory_failure_rolls_back_reservation`; manually exercise live-agent capacity recovery. |
| AC.5 | `active_turns_never_exceed_manager_limit`, `backend_poll_holds_physical_permit_while_suspension_races`, `completed_waiter_observes_ownership_during_backend_poll`, `parent_waiting_for_child_yields_permit`, `parent_cancellation_while_suspended_retires_wait_and_releases_permit`, `dropping_pending_managed_execution_closes_lease_and_releases_permit`, strengthened `two_child_waits_share_one_parent_suspension`, and `watcher_registration_failure_cancels_exact_admitted_child_turn`. The complete manager module also passes 20/20 combined stress iterations. |
| AC.6 | `cancel_existing_catches_turn_between_pop_and_active_install`, `cancel_turn_catches_exact_turn_between_pop_and_active_install`, `cancel_agent_is_reusable_and_isolates_siblings`, `cancel_subtree_marks_reserved_descendant_and_preserves_reuse`, reserved-root close/shutdown race tests, and `shutdown_timeout_reaper_eventually_joins_timed_out_node`. |
| AC.7 | `duplicate_compatibility_ids_route_by_agent_id`, `subagent_completion_queues_each_distinct_history_once`, and `subagent_closed_prunes_only_its_delivery_state`; audit `live_chat_index`, child channels, stamped outcomes, history, input, and child selection for `AgentId` keys while retaining presentation strings only in compatibility paths. |
| AC.8 | `managed_session_uses_root_authority_and_closes_its_node` asserts history carries the committed child `AgentId`; `managed_prompt_timeout_returns_pair_closes_child_and_resumes_parent` covers the connected managed Lua timeout boundary; `repeated_close_and_drop_emit_subagent_closed_once` covers idempotent notification; `failed_subagent_turn_resolves_and_same_session_recovers` plus the actor setup-failure ordering assertion prove ticket completion exposes idle status and failure detail; `duplicate_task_id_is_rejected_without_overwrite`, `spawn_returns_task_id_immediately`, `send_queues_and_reactivates`, `despawn_releases_permit_and_clears_task`, `async_turns_acquire_capacity_only_when_the_driver_runs_them`, `despawn_cancels_a_turn_waiting_for_capacity`, and `despawn_releases_capacity_from_an_active_turn`. |
| AC.9 | `relay_session_events_preserves_nested_subagent_identity_and_history`, `task_tools_allow_general_children_but_not_research_children`, and the nested-agent manual smoke test for depth/fan-out enforcement and capacity recovery. |
| AC.10 | Existing actor/root regressions plus `reusable_subagent_processes_distinct_turn_outcomes`, `main_turn_completion_keeps_async_subagent_cancellable_and_reusable`, `typing_in_subagent_chat_edits_input_and_submits_to_subagent`, `paste_in_subagent_chat_edits_input_and_submits_to_subagent`, `subagent_completion_queues_each_history_growth_once`, `grandchild_history_is_visible_but_not_promoted_to_root`, and `subagent_closed_marks_despawned_and_rejects_input`. |
| AC.11 | Focused source audit of production `AgentActorHandle::spawn`, manager graph key types, `CancelMap<String>`, and compatibility-id uses against the explicit unmanaged/deferred allowlist. |
| AC.12 | Passed scoped actor/Lua/manager/storage/UI checks, full `maki-agent`, full `maki-ui` (1595 tests), formatting, clippy with warnings denied, generated-doc checks, release build, diff whitespace checks, and final `.ssh/remote-ci.sh` (5028/5028 tests plus Stylua, Ruff, Ty, generated docs, and cargo-machete). The complete manager module passed 20/20 stress iterations; the exact package-scoped storage regressions pass without feature-order assumptions; the earlier actor status race stress check passed 100/100. |

Run the cheapest relevant test after each phase. Before broad adapter edits, compile a minimal manager-created TUI root and manager-created Lua child to expose object-safety, `Send`, lifetime, and cyclic ownership issues early. If an untouched workspace test fails, rerun its exact filter once to classify flakiness and report both results; never weaken unrelated tests.

### Review Strategy

The pre-handoff plan review and four implementation review passes were completed. Every actionable finding was verified against the code before correction; unsupported or deliberate residuals stayed documented rather than being changed speculatively. The first implementation review drove lifecycle-ordering, factory-unwind rollback, replacement-state, lock-loss/startup-rollback, managed-identity, and compatibility-shutdown corrections. The second review approved those fixes and found the package-scoped storage fixture ordering defect. The third review found and drove fixes for poll-local physical-permit ownership, prepared replacement after lock loss, off-thread joinable heartbeats, race-free release-marker semantics, subtree/abnormal-drop/cancellation coverage, Lua lifecycle cleanup, and content-exact history delivery deduplication. The fourth review found that prompt waiters could mistake a poll-local permit move for lost logical ownership and miss the later restoration event, and that advisory-lock contention was conflated with actual lease loss. The fixes added explicit logical permit ownership with restoration notification, propagated `WouldBlock` through the existing transient heartbeat error path, and added deterministic regressions for both interleavings while retaining separate owner-replacement coverage. Each correction received focused verification before broad CI; the final remote `just ci` run passed 5030/5030 tests, formatting, clippy, Lua and Python checks, generated docs, and dependency hygiene.

During implementation:

1. The execute lead fixes the public manager graph/factory/capability/permit interfaces and their invariants before delegation. Delegate only non-overlapping bounded slices after that boundary compiles: manager core/tests, config/generated docs, TUI adapter, and Lua compatibility integration. Do not let separate agents independently invent overlapping manager APIs.
2. Require every implementation delegate to use an exclusive file allowlist, report exact changes/tests, avoid unrelated formatting, and stop on required out-of-scope edits. The execute lead reviews every diff and runs focused checks before dependent work begins.
3. After all automatable tests pass, dispatch a `general-purpose` implementation reviewer focused on graph lock ordering, factory rollback, capability replay/wrong-manager attacks, manager/actor ownership cycles, permit leaks and recursive deadlock, spawn-versus-cancel cuts, exact lifecycle outcomes, compatibility-id leakage into authority, and accidental PR 4/5/6/8/9 scope.
4. Fix or explicitly rebut all findings. If any critical finding remains, run another implementation review after fixes until none remain or report a concrete blocker to the operator.

### Documentation Strategy

Add concise module/type docs in `maki-agent/src/manager/` describing factory ownership, topology-not-authority, and lock/await rules.

Add generated `[agent]` configuration reference entries for the four limits and update the task plugin option documentation to explain that `plugins.task.max_concurrent` is deprecated and applies only to unmanaged compatibility sessions. Do not publish first-class Lua agent API, graph navigation, persistence, preset, or mode documentation before those features exist. Run `just gen-docs-check` and inspect generated changes.

### Risks, Blockers, and Required Decisions

- **Settled limits:** Use the operator-selected balanced defaults: 8 active turns, depth 4 with root depth 0, 16 live direct children, and 64 live nodes including root. Closed/removed tombstones do not count; reusable terminal turns do not close nodes; nesting is allowed until PR 4 policy narrows it.
- **Manager versus actor ownership:** The manager must not duplicate actor queue/history/outcome state. Keep actor tasks manager-owned without an `Arc` cycle: runtime tasks and backend contexts hold weak manager references or narrowly scoped capabilities. `WatcherRegistry` owns active watcher handles outside `TurnPermitLease`; terminal watchers may remove their entry into a detached join, while shutdown drains remaining handles. The lease stores only ids/triggers/completion state, and `ManagedExecutionFuture::drop` closes registration, signals watchers, releases any permit, then revokes the turn guard.
- **Factory rollback race:** Actor/backend construction can return an error or unwind after graph reservation. The manager catches factory unwind, translates it to `ManagerError::Factory`, and rolls the reservation back idempotently so root creation can retry, child capacity is restored, and a concurrent close/shutdown cannot resurrect the node.
- **Capability staging:** PR 3’s opaque turn-bound capability is deliberately narrower than the comprehensive PR 4 policy/grant design, but its interface must support later ceiling intersection. Do not expose `Arc<AgentManager>` as unrestricted authority through `ToolContext` or Lua.
- **Concurrency deadlock:** Counting root and child active turns under the shared cap requires suspension. A synchronous guard cannot reacquire in `Drop`; correctness lives in `ManagedExecutionFuture`’s asynchronous pre-poll gate. Suspension drop only marks/wakes, and every later backend poll requires physical ownership. Shutdown retains manager/limiter state through any detached reaper.
- **Cancellation semantics:** Reusable interactive cancellation and permanent task despawn are distinct. `cancel_agent`/`cancel_subtree` leave nodes open; close/remove/shutdown reject future work/spawn. First terminal/cancellation state still wins inside each PR 2 actor.
- **Compatibility identity:** Duplicate sessions can share a `tool_use_id`; any compatibility mapping must be one-to-many or node-local. Never make a string tool id a unique graph key. Restored static child tabs have no live node until PR 6 and must remain display-only.
- **Outer-session replacement:** App construction/reset/load/rewind previously had shared side effects before action handling. PR 3 uses immutable requests plus inert `SpawnCtx::prepare_runtime` and no-await activation. Prepared command targets and mailboxes stay unpublished; stored permission rules load during preparation even when history is empty; target lock claim is the final fallible precommit step; same-id guards and host flags such as `exit_on_done` transfer without I/O. Startup failure releases earlier claims. A runtime that loses lock ownership stops with an error and skips checkpoint/replacement so it cannot overwrite the current owner. There is no in-place root replacement API.
- **Lua invocation scope:** `LuaSession::prompt` has no `ctx` parameter and sessions outlive creation turns. It must read the caller’s current managed token/lease from the existing `TaskCell`/`TaskScope`/`ScopedFuture` invocation scope at call time. Never retain creation-turn authority in the session; detached scopes carry none.
- **TUI testability:** Add a scoped `RuntimeHarness` for cheap prepared/active runtime construction, publication boundaries, manager/root identity, deferred restore, lock transfer, and final target-lock failure. Keep preparation infallible after provider resolution instead of introducing artificial per-stage failure points.
- **Deferred frontends:** Headless, ACP, SDK, and print still construct low-level agents directly by design until PR 9. Keep the exception explicit in code/docs/structural tests rather than weakening the manager-only invariant for migrated TUI sessions.
- **Task option migration:** Keep `plugins.task.max_concurrent` parsed and deprecated, with no alias because it is process-scoped. It remains effective only as the unmanaged-session fallback; managed sessions use only `[agent].max_concurrent_agent_turns`. The session adapter, not two plugin layers, conditionally acquires the fallback.
- **No persistence yet:** Graph snapshots and tombstones are in-memory projections only. Do not serialize them or reinterpret legacy stored subagent tabs; PR 6 will define durable identity and restart behavior.
- **Working tree:** Preserve all unrelated session-start untracked files and do not include them in implementation commits.